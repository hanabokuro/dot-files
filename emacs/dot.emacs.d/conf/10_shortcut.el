(defalias 'qrr 'query-replace-regexp)
(defalias 'cr  'comment-region)
(defalias 'mgt  'magit-status)
(defalias 'git  'magit-status)
(defalias 'mt   'migemo-toggle-isearch-enable)
(defalias 'uc   'universal-coding-system-argument)

